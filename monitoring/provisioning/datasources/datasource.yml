apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090  # Nom du service docker
    isDefault: true
    editable: false # Empêche la modif manuelle dans l'UI